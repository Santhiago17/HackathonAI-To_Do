// Configurações para o uso de dados mockados
export const mockConfig = {
  // Define se os mocks estão ativos ou não
  useMockData: false,

  // Define o delay simulado para as chamadas de API (em ms)
  apiDelay: 500
}
